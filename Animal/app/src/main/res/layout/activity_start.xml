<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_start"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/pikalong"
    tools:context="com.mtsoft.animal.activity.StartActivity">

    <ImageView
        android:id="@+id/ic_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="@dimen/_20sdp"
        android:src="@drawable/ic_3" />

    <TextView
        android:id="@+id/ic_fruit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/ic_number"
        android:layout_margin="@dimen/_10sdp"
        android:background="@drawable/ic_start"
        android:gravity="center"
        android:text="@string/start"
        android:textColor="@android:color/white"
        android:textSize="24sp"
        android:textStyle="bold"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/ic_alphabet"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/ic_number"
        android:layout_alignParentRight="true"
        android:layout_margin="@dimen/_10sdp"
        android:background="@drawable/ic_alphabet"
        android:gravity="center"
        android:visibility="invisible"
        android:text="@string/exit"
        android:textColor="#2196F3"
        android:textSize="24sp"
        android:textStyle="bold" />
</RelativeLayout>
